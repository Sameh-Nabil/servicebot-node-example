<div>
    <% if(locals.user){ %>
        <a href="logout">Logout</a>
        <a href="profile">Profile</a>
    <% }else{ %>
        <a href="login">Login</a>
        <a href="signup">Sign Up</a>
    <% } %>
</div>